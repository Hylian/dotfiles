# 2026-07-17: chez initialization ٩(◕‿◕｡)۶

## Overview
Initialized internal tracking space for `chez`.

## Repository Profile
- **Target OS:** Linux (Arch/other) & macOS (Darwin).
- **Core WMs / Compositors:** `sway`, `niri` (Linux) | `aerospace` (macOS).
- **Key Tools & UIs:** `nvim`, `zellij`, `ghostty`, `waybar`, `sketchybar`, `borders`, `dunst`, `fzf`, `ripgrep`, `starship`, `zsh`.
- **Operating Guidelines:**
  - Concise, casual, upbeat style with kaomoji.
  - Timestamped markdown notes in `notes/` for memory and design context.
  - Zero secrets/tokens/work-specific private paths in this repo or notes.
  - Proactive `chezmoi apply` within reason; confirm disruptive changes.
  - Commit all meaningful changes to git for easy rollbacks (no autonomous git push).
